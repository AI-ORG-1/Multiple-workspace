
jobs:
  include:
    - stage: warm_cache
      script:
        - echo "foo" > foo.txt
      workspaces:
        create:
          name: ws1
          paths:
            - foo.txt
      workspaces:
        create:
          name: ws2
          paths:
            - goo.txt
      workspaces:
        create:
          name: ws3
          paths:
            - goo.txt
    - stage: use_cache
      workspaces:
        use: ws2
      script:
        - cat goo.txt || true
  
